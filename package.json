{
  "name": "fanqie-download",
  "version": "2.3.0",
  "private": true,
  "description": "番茄小说搜索与下载（静态前端 + Vercel Serverless API）",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "mkdir -p public && cp -f index.html reader.html reader.js styles.css app.js browser-client.js charset.json public/ && cp -r Speech public/ 2>/dev/null || true"
  },
  "engines": {
    "node": ">=18"
  }
}
